# API 클라이언트 (Axios) 설정

## 예상 소요시간
| 구분 | 시간 |
|------|------|
| 사람 (숙련 개발자) | ~30분 |
| AI 에이전트 | ~10분 |

## What (무엇을)
Axios 인스턴스를 생성하여 모든 API 호출의 기반을 마련합니다. 요청 인터셉터로 JWT 토큰을 자동 첨부하고, 응답 인터셉터에서 401 토큰 만료 시 refresh 토큰으로 자동 갱신합니다. 응답을 APIResponse 타입으로 언래핑합니다.

## Why (왜)
모든 API 호출에 일관되게 인증 토큰을 첨부하고, 토큰 만료 시 자동으로 갱신하여 사용자 경험을 개선합니다. XSS 공격을 방지하기 위해 토큰은 메모리(변수)에만 저장합니다.

## 선행 작업
- `11.프론트엔드-공통/01.라우팅-설정.md`

## 수행 가이드

### Step 1: Axios 패키지 설치
```bash
cd /sessions/fervent-charming-hopper/mnt/한국\ 공공\ IT\ 발주\ 웹서비스화/apps/frontend
npm install axios
```

### Step 2: API 응답 타입 정의
`apps/frontend/src/types/api.ts` 파일을 생성합니다:

```typescript
export interface APIResponse<T = any> {
  code: string;
  message: string;
  data: T;
}

export interface PaginatedResponse<T> {
  items: T[];
  total: number;
  page: number;
  pageSize: number;
}

export interface ErrorResponse {
  code: string;
  message: string;
  details?: Record<string, any>;
}

// 기본 사용자 데이터 타입
export interface UserDto {
  id: string;
  email: string;
  name: string;
  role: 'ADMIN' | 'PM' | 'STAFF';
  company?: string;
  createdAt: string;
}
```

### Step 3: 토큰 저장소 생성
`apps/frontend/src/services/tokenStore.ts` 파일을 생성합니다:

```typescript
// 메모리에만 저장 (localStorage 사용 안 함)
let accessToken: string | null = null;
let refreshToken: string | null = null;

export const tokenStore = {
  getAccessToken: () => accessToken,
  setAccessToken: (token: string) => {
    accessToken = token;
  },
  getRefreshToken: () => refreshToken,
  setRefreshToken: (token: string) => {
    refreshToken = token;
  },
  clear: () => {
    accessToken = null;
    refreshToken = null;
  },
};
```

### Step 4: Axios 인스턴스 및 인터셉터 생성
`apps/frontend/src/services/api.ts` 파일을 생성합니다:

```typescript
import axios, {
  AxiosInstance,
  AxiosRequestConfig,
  AxiosResponse,
  InternalAxiosRequestConfig,
} from 'axios';
import { APIResponse, ErrorResponse } from '../types/api';
import { tokenStore } from './tokenStore';

const API_BASE_URL = '/api/v1';

// Axios 인스턴스 생성
const apiClient: AxiosInstance = axios.create({
  baseURL: API_BASE_URL,
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 요청 인터셉터: JWT 토큰 자동 첨부
apiClient.interceptors.request.use(
  (config: InternalAxiosRequestConfig) => {
    const token = tokenStore.getAccessToken();
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 응답 인터셉터: 401 에러 처리 및 토큰 갱신
let isRefreshing = false;
let refreshSubscribers: ((token: string) => void)[] = [];

const onRefreshed = (token: string) => {
  refreshSubscribers.forEach((callback) => callback(token));
  refreshSubscribers = [];
};

const addRefreshSubscriber = (callback: (token: string) => void) => {
  refreshSubscribers.push(callback);
};

apiClient.interceptors.response.use(
  (response: AxiosResponse<APIResponse>) => {
    // 응답 데이터 언래핑
    return response.data as any;
  },
  async (error) => {
    const originalRequest = error.config;

    // 401 Unauthorized 처리
    if (error.response?.status === 401) {
      const errorCode = error.response?.data?.code;

      // 토큰 만료 에러 코드 확인
      if (errorCode === '401001' && !isRefreshing) {
        isRefreshing = true;

        try {
          // Refresh 토큰으로 새 access 토큰 요청
          const refreshToken = tokenStore.getRefreshToken();
          if (!refreshToken) {
            throw new Error('No refresh token');
          }

          const response = await axios.post(
            `${API_BASE_URL}/auth/refresh`,
            { refreshToken },
            { headers: { 'Content-Type': 'application/json' } }
          );

          const newAccessToken = response.data.data.accessToken;
          const newRefreshToken = response.data.data.refreshToken;

          tokenStore.setAccessToken(newAccessToken);
          tokenStore.setRefreshToken(newRefreshToken);

          isRefreshing = false;
          onRefreshed(newAccessToken);

          // 원래 요청 재시도
          originalRequest.headers.Authorization = `Bearer ${newAccessToken}`;
          return apiClient(originalRequest);
        } catch (refreshError) {
          isRefreshing = false;
          tokenStore.clear();
          window.location.href = '/login';
          return Promise.reject(refreshError);
        }
      } else if (errorCode === '401001') {
        // 토큰 갱신 중이면 대기
        return new Promise((resolve) => {
          addRefreshSubscriber((token: string) => {
            originalRequest.headers.Authorization = `Bearer ${token}`;
            resolve(apiClient(originalRequest));
          });
        });
      } else {
        // 기타 인증 에러
        tokenStore.clear();
        window.location.href = '/login';
      }
    }

    return Promise.reject(error);
  }
);

export default apiClient;
```

### Step 5: API 서비스 클래스 생성 (선택사항)
`apps/frontend/src/services/authService.ts` 파일을 생성합니다:

```typescript
import apiClient from './api';
import { APIResponse, UserDto } from '../types/api';
import { tokenStore } from './tokenStore';

interface LoginRequest {
  email: string;
  password: string;
}

interface LoginResponse {
  accessToken: string;
  refreshToken: string;
  user: UserDto;
}

interface RegisterRequest {
  email: string;
  password: string;
  name: string;
  tel?: string;
}

export const authService = {
  login: async (data: LoginRequest): Promise<LoginResponse> => {
    const response = await apiClient.post<APIResponse<LoginResponse>>(
      '/auth/login',
      data
    );
    const loginData = response.data;

    // 토큰 저장
    tokenStore.setAccessToken(loginData.accessToken);
    tokenStore.setRefreshToken(loginData.refreshToken);

    return loginData;
  },

  register: async (data: RegisterRequest): Promise<LoginResponse> => {
    const response = await apiClient.post<APIResponse<LoginResponse>>(
      '/auth/register',
      data
    );
    const registerData = response.data;

    tokenStore.setAccessToken(registerData.accessToken);
    tokenStore.setRefreshToken(registerData.refreshToken);

    return registerData;
  },

  logout: () => {
    tokenStore.clear();
  },

  refresh: async (refreshToken: string): Promise<{ accessToken: string; refreshToken: string }> => {
    const response = await apiClient.post(
      '/auth/refresh',
      { refreshToken }
    );
    return response.data;
  },
};
```

### Step 6: 프로젝트 서비스 생성 예제
`apps/frontend/src/services/projectService.ts` 파일을 생성합니다:

```typescript
import apiClient from './api';
import { APIResponse, PaginatedResponse } from '../types/api';

export interface ProjectDto {
  id: string;
  name: string;
  code?: string;
  description?: string;
  startDate: string;
  endDate: string;
  budget?: number;
  progress: number;
  daysLeft: number;
  myRole: string;
  myCompany: string;
}

export const projectService = {
  getProjects: async (page = 1, pageSize = 10): Promise<PaginatedResponse<ProjectDto>> => {
    const response = await apiClient.get(
      `/projects?page=${page}&pageSize=${pageSize}`
    );
    return response.data;
  },

  getProjectById: async (id: string): Promise<ProjectDto> => {
    const response = await apiClient.get(`/projects/${id}`);
    return response.data;
  },

  createProject: async (data: Partial<ProjectDto>): Promise<ProjectDto> => {
    const response = await apiClient.post('/projects', data);
    return response.data;
  },

  updateProject: async (id: string, data: Partial<ProjectDto>): Promise<ProjectDto> => {
    const response = await apiClient.patch(`/projects/${id}`, data);
    return response.data;
  },

  deleteProject: async (id: string): Promise<void> => {
    await apiClient.delete(`/projects/${id}`);
  },
};
```

## 완료 기준
- [ ] Axios 패키지 설치 완료
- [ ] tokenStore가 메모리에만 토큰 저장 (localStorage 미사용)
- [ ] 요청 인터셉터가 Authorization 헤더에 Bearer 토큰 자동 첨부
- [ ] 응답 인터셉터가 401001 에러 시 refresh 토큰으로 자동 갱신
- [ ] APIResponse 타입으로 응답 언래핑 동작
- [ ] 토큰 만료 후 원래 요청 자동 재시도
- [ ] 갱신 실패 시 /login으로 리다이렉트
- [ ] 토큰 갱신 중 중복 요청 방지 (isRefreshing 플래그 활용)

## 참고
- Axios 공식 문서: https://axios-http.com/
- 인터셉터 활용: https://axios-http.com/kr/docs/interceptors
- JWT 토큰 메모리 저장: XSS 공격 방지
- 다중 요청 중 토큰 갱신 시 한 번만 수행하는 최적화 포함
