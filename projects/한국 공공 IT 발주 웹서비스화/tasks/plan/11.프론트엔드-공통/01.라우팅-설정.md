# 라우팅 설정

## 예상 소요시간
| 구분 | 시간 |
|------|------|
| 사람 (숙련 개발자) | ~20분 |
| AI 에이전트 | ~8분 |

## What (무엇을)
React Router v6를 설정하여 SPA의 페이지 라우팅을 구현합니다. 각 경로에 대한 컴포넌트를 정의하고, 미인증 사용자는 자동으로 로그인 페이지로 리다이렉트되는 보호 라우트를 구현합니다.

## Why (왜)
SPA 네비게이션의 기반이 됩니다. 사용자 인증 상태에 따른 경로 제어는 애플리케이션 보안의 핵심입니다.

## 선행 작업
- `01.모노레포-프로젝트-세팅/04.프론트엔드-React-초기화.md`

## 수행 가이드

### Step 1: react-router-dom 패키지 설치
```bash
cd /sessions/fervent-charming-hopper/mnt/한국\ 공공\ IT\ 발주\ 웹서비스화/apps/frontend
npm install react-router-dom
npm install --save-dev @types/react-router-dom
```

### Step 2: ProtectedRoute 컴포넌트 생성
`apps/frontend/src/components/ProtectedRoute.tsx` 파일을 생성합니다:

```typescript
import React from 'react';
import { Navigate } from 'react-router-dom';
import { useAuth } from '../hooks/useAuth';

interface ProtectedRouteProps {
  children: React.ReactNode;
}

export const ProtectedRoute: React.FC<ProtectedRouteProps> = ({ children }) => {
  const { isAuthenticated, isLoading } = useAuth();

  if (isLoading) {
    return <div>로딩 중...</div>;
  }

  if (!isAuthenticated) {
    return <Navigate to="/login" replace />;
  }

  return <>{children}</>;
};
```

### Step 3: 라우터 설정 파일 생성
`apps/frontend/src/router/index.tsx` 파일을 생성합니다:

```typescript
import React from 'react';
import { createBrowserRouter, RouterProvider } from 'react-router-dom';
import { ProtectedRoute } from '../components/ProtectedRoute';
import { AuthLayout } from '../layouts/AuthLayout';
import { AppLayout } from '../layouts/AppLayout';
import LoginPage from '../pages/auth/LoginPage';
import RegisterPage from '../pages/auth/RegisterPage';
import ProjectListPage from '../pages/projects/ProjectListPage';
import ProjectDetailPage from '../pages/projects/ProjectDetailPage';
import WBSPage from '../pages/projects/WBSPage';
import MembersPage from '../pages/projects/MembersPage';
import DashboardPage from '../pages/projects/DashboardPage';

const router = createBrowserRouter([
  {
    path: '/login',
    element: <AuthLayout><LoginPage /></AuthLayout>,
  },
  {
    path: '/register',
    element: <AuthLayout><RegisterPage /></AuthLayout>,
  },
  {
    path: '/projects',
    element: (
      <ProtectedRoute>
        <AppLayout />
      </ProtectedRoute>
    ),
    children: [
      {
        index: true,
        element: <ProjectListPage />,
      },
      {
        path: ':id',
        element: <ProjectDetailPage />,
        children: [
          {
            path: 'dashboard',
            element: <DashboardPage />,
          },
          {
            path: 'wbs',
            element: <WBSPage />,
          },
          {
            path: 'members',
            element: <MembersPage />,
          },
        ],
      },
    ],
  },
  {
    path: '/',
    element: <Navigate to="/projects" />,
  },
]);

export const Router: React.FC = () => {
  return <RouterProvider router={router} />;
};
```

### Step 4: App.tsx에서 라우터 활성화
`apps/frontend/src/App.tsx`를 수정합니다:

```typescript
import React from 'react';
import { Router } from './router';
import { AuthProvider } from './contexts/AuthContext';

function App() {
  return (
    <AuthProvider>
      <Router />
    </AuthProvider>
  );
}

export default App;
```

### Step 5: useNavigate 훅을 사용한 프로그래매틱 네비게이션
페이지에서 프로그래매틱 네비게이션이 필요할 때:

```typescript
import { useNavigate } from 'react-router-dom';

function MyComponent() {
  const navigate = useNavigate();

  const handleProjectClick = (projectId: string) => {
    navigate(`/projects/${projectId}`);
  };

  return <button onClick={() => handleProjectClick('123')}>프로젝트 상세</button>;
}
```

## 완료 기준
- [ ] react-router-dom 패키지 설치 완료
- [ ] ProtectedRoute 컴포넌트가 미인증 시 /login으로 리다이렉트
- [ ] 라우터 설정에 모든 경로 정의 (/login, /register, /projects, /projects/:id, /projects/:id/wbs, /projects/:id/members, /projects/:id/dashboard)
- [ ] 각 경로 접근 시 해당 페이지 컴포넌트가 정상 렌더링
- [ ] 로그인 상태에서만 /projects 접근 가능
- [ ] 미인증 상태에서 보호 라우트 접근 시 /login으로 리다이렉트

## 참고
- React Router v6 공식 문서: https://reactrouter.com/
- useNavigate, useParams, useLocation 등의 훅 활용
- 향후 추가 라우트가 필요하면 children 배열에 확장
